class Solution:
    def buildArray(self, nums: List[int]) -> List[int]:
        num = []
        # a=[]
        for i in nums:
            # num=nums[i]
            num.append(nums[i])
        return(num)
            